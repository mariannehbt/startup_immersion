<!-- <div class="container-fluid"> -->

  <h1 class="my-5 text-center">LES IMMERSIONS</h1>
  <div class ="container">
    <div class="row " style="align-items:stretch">
  <!-- Grid column -->
  <div class="mb-3  pics animation mdb-lightbox-ui "></div>
<!-- <div  class="card-row"> -->

      <% @events.each do |event| %>

          <div class=" p-3 col-lg-4 col-sm-12 ">
            <div class="card p-3 shadow">
              <img class="card-img-top  img-border" src="<%= asset_path 'photo2.jpg' %>" alt="Card image cap"/>
              <div class="center mt-2"><%= event.title.upcase %></div>
                <div class="card-body">
                  <ul class="none">
                      <li><%= event.start_datetime %></li>
                      <li><%= event.duration %> min</li><br />
                      <li class="card-text"><%= event.description %></li><br />
                      <li><%= event.short_location %></li>
                        <!-- if user = register -->
                     <!--
                      <% if current_user %> 
                        <li>
                      <li><%= event.adress %></li>
                          <%= event.zip_code %>
                          <%= event.city %>
                        </li>
                      <%end%>
                      -->
                  </ul><br />
                </div>
                <div class="card-foot center">
                  <p class="card-text">
                    <%= link_to "EN VOIR +", event_path(event.id), :class =>'btn btn-sm btn-primary'%>
                    <%= link_to "SE PRÉ-INSCRIRE", "#", :class =>'btn btn-sm btn-primary'%>
                  </p><br />
                </div>
              </div>
            </div>
          <% end %>
<!-- </div> -->
    </div>
  </div>
<%# <%= link_to "Details &raquo;".html_safe, user_path(event_attendances), :class =>'btn btn-sm btn-primary' %> %>
</body>
<div class="center mb-5">
    <% if current_user && current_user.admin? %>
      <%= link_to 'New Event', new_admin_event_path %>
      <%= link_to 'Back', :admin_root %>
    <% else %>
      <%= link_to "<< BACK", :back %>
    <% end %> 
</div>

