<<<<<<< HEAD
<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @user.email %></br>
  <%= link_to "Edit email / password", edit_user_registration_path %>
</p>

<p>
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Situation:</strong>
  <% if @user.situation == nil %>
    <%= @user.situation %>
  <% else %>
    <%= @user.situation.title %>
  <% end %>
</p>

<p>
  <strong>Formation:</strong>
  <%= @user.formation %>
</p>

<p>
  <strong>Activity:</strong>
  <% if @user.activity == nil %>
    <%= @user.activity %>
  <% else %>
    <%= @user.activity.name %>
  <% end %>
</p>

<p>
  <strong>Description:</strong>
  <%= @user.description %>
</p>

<p>
  <strong>Linkedin:</strong>
  <%= @user.linked_in_url %>
</p>

<p>
  <strong>Newsletter:</strong>
  <% if @user.newsletter == nil %>
    <%= @user.newsletter %>
  <% else %>
    <%= @user.newsletter.created_at %>
  <% end %>
</p>

<p>
  <strong>Member since:</strong>
  <%= @user.created_at %>
</p>

<p>
  <strong>Last profile update:</strong>
  <%= @user.updated_at %>
</p>

<% if @user.first_name != nil %>
  <h2>Registration of <%= @user.first_name %> :</h2>
<% else %>
   <h2>Registration of this user :</h2>
<% end %>

<h3>In the future</h3>
<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Date</th>
      <th>Attendance</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users_attendances = @user.events.order('start_datetime ASC') %>
    <% @users_attendances.each do |user_attendances| %>
    <% if user_attendances.is_future? %>
      <% att = Attendance.find_by(user_id: @user, event_id: user_attendances) %>
      <tr>
        <td><%= link_to "#{user_attendances.title} &raquo;".html_safe, event_path(user_attendances), :class =>'btn btn-primary' %></td>
        <td><%= user_attendances.start_datetime %></td>
        <td><%= link_to "Details &raquo;".html_safe, event_attendance_path(user_attendances, att.id), :class =>'btn btn-primary' %></td>
        <%if att.reviewed? && att.validated? %>
          <td>Inscription confirmée !</td>
        <%elsif att.reviewed? && att.validated? == false %>
          <td>Pré-inscription non retenue</td>
        <% else %>
          <td>Pré-inscription en attente de validation</td>
        <% end %>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<h3>In the past</h3>
<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Date</th>
      <th>Attendance</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users_attendances = @user.events.order('start_datetime ASC') %>
    <% @users_attendances.each do |user_attendances| %>
    <% if user_attendances.is_future? == false %>
      <% att = Attendance.find_by(user_id: @user, event_id: user_attendances) %>
      <tr>
        <td><%= link_to "#{user_attendances.title} &raquo;".html_safe, event_path(user_attendances), :class =>'btn btn-primary' %></td>
        <td><%= user_attendances.start_datetime %></td>
        <td><%= link_to "Details &raquo;".html_safe, event_attendance_path(user_attendances, att.id), :class =>'btn btn-primary' %></td>
        <%if att.reviewed? && att.validated? %>
          <td>Inscription confirmée !</td>
        <%elsif att.reviewed? && att.validated? == false %>
          <td>Pré-inscription non retenue</td>
        <% else %>
          <td>Pré-inscription en attente de validation</td>
        <% end %>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<h3>Photo de profil</h3>
<%if @user.avatar.attached?%>
  <%= image_tag @user.avatar, alt: 'avatar' %>
<%else%>
  <p>=== Il n'y a pas encore de photo de profil lié à cet utilisateur ===</p>
<%end%>
<h3>Changer de photo de profil ?</h3>
<%= form_tag user_avatars_path(@user), multipart: true do %>
  <%= file_field_tag :avatar %>
  <%= submit_tag "Mettre à jour" %>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<% if current_user.admin? %>
  <%= link_to 'Back', :back %>
<% else %>
  <%= link_to 'Back', :root %>
<% end %>
=======
    <p id="notice"><%= notice %></p>

<div class="container"> <br /> <br />

    <p>
      <strong>Email:</strong>
      <%= @user.email %><br/>
      <%= link_to "Edit email / password", edit_user_registration_path %>
    </p>

    <p>
      <strong>First name:</strong>
      <%= @user.first_name %>
    </p>

    <p>
      <strong>Last name:</strong>
      <%= @user.last_name %>
    </p>

    <p>
      <strong>Situation:</strong>
      <% if @user.situation == nil %>
        <%= @user.situation %>
      <% else %>
        <%= @user.situation.title %>
      <% end %>
    </p>

    <p>
      <strong>Formation:</strong>
      <%= @user.formation %>
    </p>

    <p>
      <strong>Activity:</strong>
      <% if @user.activity == nil %>
        <%= @user.activity %>
      <% else %>
        <%= @user.activity.name %>
      <% end %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= @user.description %>
    </p>

    <p>
      <strong>Linkedin:</strong>
      <%= @user.linked_in_url %>
    </p>

    <p>
      <strong>Newsletter:</strong>
      <%= @user.newsletter %>
    </p>

    <p>
      <strong>Member since:</strong>
      <%= @user.created_at %>
    </p>

    <p>
      <strong>Last profile update:</strong>
      <%= @user.updated_at %>
    </p>

    <% if @user.first_name != nil %>
      <h2><strong>Registration of <%= @user.first_name %> :</strong></h2>
    <% else %>
       <h3><strong>Registration of this user :</strong></h3>
    <% end %><br />

    <% @user.events.each do |user_attendances| %>
    <ul class="none">
        <li>Event:    <%= user_attendances.title %></li>
        <li>Date:     <%= user_attendances.start_datetime %></li>
        <li><%= link_to "Details &raquo;".html_safe, event_path(user_attendances), :class =>'btn btn-sm btn-outline-secondary' %> </li>
        <li> </li>
    </ul> <br />
    <% end %>


    <h3><strong>Profil Page</strong></h3>
    <p>Bienvenue sur la page Photo de profil<%=@user.id%></p>

    <h3>Avatar actuel</h3>
    <%if @user.avatar.attached?%>
      <%= image_tag @user.avatar, alt: 'avatar' %>
    <%else%>
      <p>=== il n'y a pas encore d'avatar ===</p>
    <%end%> <br />

    <h3>Switch picture profile </h3>
    <%= form_tag user_avatars_path(@user), multipart: true do %>
      <%= file_field_tag :avatar%>

      <%= submit_tag "valider", :class => 'btn btn-success'%>
    <% end %> <br />

    <%= link_to 'Edit', edit_user_path(@user) %> |
    <%= link_to 'Back', users_path %>
</div>

<br/>
<br/>
>>>>>>> front
